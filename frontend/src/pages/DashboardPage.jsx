import Navbar from '@/components/layout/Navbar';

// Placeholder for now — task list, filters, pagination, and analytics
// get built into this page in the next steps.
export default function DashboardPage() {
  return (
    <div className="min-h-svh bg-background">
      <Navbar />
      <main className="mx-auto max-w-6xl px-4 py-6">
        <p className="text-muted-foreground">Dashboard coming next.</p>
      </main>
    </div>
  );
}
