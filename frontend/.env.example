# Copy this file to .env and adjust if needed. Never commit .env.

# Base URL the frontend calls for the API. In dev this is proxied to the
# backend by Vite (see vite.config.js), so /api works without CORS setup.
# Must be prefixed with VITE_ or Vite won't expose it to client code.
VITE_API_URL=/api
