{
  "name": "task-tracker",
  "version": "1.0.0",
  "description": "",
  "homepage": "https://github.com/RushilReddy909/Task-Tracker#readme",
  "bugs": {
    "url": "https://github.com/RushilReddy909/Task-Tracker/issues"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/RushilReddy909/Task-Tracker.git"
  },
  "license": "MIT",
  "author": "Rushil Reddy",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "build": "npm run build --if-present --prefix backend && npm run build --if-present --prefix frontend",
    "dev": "concurrently -n backend,frontend -c blue,green \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "start": "npm start --prefix backend",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "concurrently": "^10.0.5"
  }
}
